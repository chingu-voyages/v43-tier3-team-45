import LoginButton from "./LoginButton"
import LogoutButton from "./LogoutButton"

const LoginForm = () => {

    return (
        <div>
            < LoginButton/>
            < LogoutButton />
        </div>
    )

}

export default LoginForm